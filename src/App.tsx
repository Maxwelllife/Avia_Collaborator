import {FC} from 'react';
import {TicketsList} from "components";

const App: FC = () => {
    return (
        <div className="App">
            <TicketsList/>
        </div>
    );
}

export default App;
